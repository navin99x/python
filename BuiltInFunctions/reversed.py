# returns an iterator that accesses elements of an iterable in reverse order

numbers = [1, 2, 3, 4]
rev_numbers = reversed(numbers)
print("Reversed list:", list(rev_numbers))  # Output: Reversed list: [4, 3, 2, 1]
